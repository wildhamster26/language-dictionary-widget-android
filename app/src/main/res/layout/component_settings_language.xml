<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="12dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/language_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/placeholder_label"
            android:textColor="@color/word_native"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/language_sublabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/placeholder_sublabel"
            android:textColor="@color/text_hint"
            android:textSize="14sp" />

    </LinearLayout>

    <TextView
        android:id="@+id/language_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_control_box"
        android:gravity="center"
        android:minWidth="80dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:text="@string/placeholder_language"
        android:textColor="@color/word_native"
        android:textSize="14sp" />

</LinearLayout>
